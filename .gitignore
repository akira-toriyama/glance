# build artifacts
/bin/
/.build/

# release artifacts
/dist/
/RAW_NOTES.md
/RELEASE_NOTES.md

# persistent signing identity (per-machine)
.signing-id

# macOS
.DS_Store

# Claude Code harness runtime (ScheduleWakeup lock 等)
.claude/
